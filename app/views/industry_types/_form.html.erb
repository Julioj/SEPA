<%= form_for(industry_type) do |f| %>
  <% if industry_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(industry_type.errors.count, "error") %> prohibited this industry_type from being saved:</h2>

      <ul>
      <% industry_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
