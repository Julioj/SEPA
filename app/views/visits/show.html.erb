<p id="notice"><%= notice %></p>

<p>
  <h1> <%= VisitType.find(@visit.visitTypeId).name %> </h1>
</p>
<p>
  <strong>Fecha de la visita:</strong>
  <%= @visit.visitDate %>
  <strong>Cliente:</strong>
  <%= Company.find(@visit.companyId).name %>
</p>

<p>
  <strong>Responsable:</strong>
  <%= Employee.find(@visit.employeeId).name %>
  <strong>Frecuencia:</strong>
  <%= Frecuency.find(@visit.frecuencyTypeId).name %>
</p>

<p>
  <strong>Aprobado:</strong>
  <%=  @visit.aproved ? 'Si':'No'%>
</p>

<h3>Documentos</h3>

<table class="pure-table pure-table-horizontal">
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @documents.each do |document| %>
      <tr>
        <td><%= document.name %></td>
        <td><%= link_to 'Descargar', document.images.url(:original, false) %></td>
        <td><%= link_to 'Editar', edit_document_path(document) %></td>
        <td><%= link_to 'Eliminar', document, method: :delete, data: { confirm: 'Esta Ud Seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Editar', edit_visit_path(@visit) %> |
<%= link_to 'Regresar', visits_path %>
